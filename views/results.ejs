<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    <title>Výsledky ankety</title>
</head>
<body>
    <h1>Výsledky ankety</h1>
    <h2>Anketa o fotbalovém Euru 2024</h2>
    <%- include('blocks/header'); -%>
    <div class="container">
        <table>
        <tr>
            <th>ID</th>
            <th>Datum</th>
            <th>Čas</th>
            <th>Místo České reprezentace</th>
            <th>Oblíbená pozice</th>
            <th>Hodnocení ankety</th>
            <th>Tým kterému fandíte</th>
        </tr>
        <% responses.forEach(function(response) { 
            const date = new Date(response.timestamp);
            const dateString = `${('0' + date.getDate()).slice(-2)}.${('0' + (date.getMonth() + 1)).slice(-2)}.${date.getFullYear()}`;
            const timeString = `${('0' + date.getHours()).slice(-2)}:${('0' + date.getMinutes()).slice(-2)}:${('0' + date.getSeconds()).slice(-2)}`;
        %>
            <tr>
                <td><%= response.id %></td>
                <td><%= dateString %></td>
                <td><%= timeString %></td>
                <td><%= response.answers.hours %></td>
                <td><%= response.answers.sport %></td>
                <td><%= response.answers.importance %></td>
                <td><%= response.answers.roleTeam %></td>
            </tr>
        <% }); %>
        </table>
    </div>  
</body>
<footer>
    <p>&copy; 2024 Barbora Žemličková IT-2 , Projekt Webová anketa</p>
</footer>
</html>